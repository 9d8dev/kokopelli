import InlineLink from "./inline-link";

const Nav = () => {
  return (
    <nav className="py-6 max-w-7xl m-auto sticky top-0">
      <InlineLink href="/">Kokopelli.dev</InlineLink>
    </nav>
  );
};

export default Nav;
